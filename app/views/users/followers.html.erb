<div class="container">
  <div class="row">
    <h2>Followers</h2>
    <% if @users.exists? %>
      <%= render 'index', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>