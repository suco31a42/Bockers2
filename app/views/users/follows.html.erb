<div class="container">
  <div class="row">
    <h2>Follows</h2>
    <% if @users.exists? %>
      <%= render 'index', users: @users %>
    <% else %>
      <p>フォローしているユーザーはいません</p>
    <% end %>
  </div>
</div>
